const emailValidator = require('./email-validator');
const passwordValidator = require('./password-validator');
const idGenerator = require('./id-generator');

module.exports = { emailValidator, passwordValidator, idGenerator };